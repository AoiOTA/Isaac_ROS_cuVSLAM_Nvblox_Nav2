[project]
name = "nova-carter-visual-navigation"
version = "0.1.0"
description = "Isaac Sim and ROS 2 visual navigation for Nova Carter"
requires-python = ">=3.12"

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-ra"

[tool.ruff]
line-length = 100
target-version = "py312"
