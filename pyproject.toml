[project]
name = "kujiale-jackal-visual-navigation"
version = "0.1.0"
description = "Kujiale and Jackal visual navigation with eight-camera mapping"
requires-python = ">=3.12"

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-ra"

[tool.ruff]
line-length = 100
target-version = "py312"
