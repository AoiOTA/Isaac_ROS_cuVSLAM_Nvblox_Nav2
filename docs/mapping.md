# Mapping

Planned map output root: `data/maps/warehouse_v1`.

The initial workflow will save cuVSLAM, nvblox, mesh, occupancy, and cuVGL artifacts from one automated closed-loop collection run. Native Isaac Sim depth is the nvblox depth source; FoundationStereo and ESS are not part of the baseline.
